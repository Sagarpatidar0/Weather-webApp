name: Node.js CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
          path: ./ 

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 14

    - name: Install Dependencies
      run: npm install

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Deploy to Production
      # Add your deployment script or commands here
      run: |
        echo "Deployment script or commands go here"
        ls
        cd ../
        ls
        cd ../
        ls 
        cd ../
        ls
        cd ../
        ls
        cd ../
        ls
